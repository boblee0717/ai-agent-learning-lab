"""Reference solutions. Not collected by pytest by default.

Open these only after attempting the exercise yourself. To run a solution
against its tests, replace the matching file under ``exercises/starter/``
with the corresponding solution (or symlink it).
"""
